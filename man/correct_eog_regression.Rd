% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correct_eog.R
\name{correct_eog_regression}
\alias{correct_eog_regression}
\title{Remove EOG artefacts using linear regression}
\usage{
correct_eog_regression(
  psg,
  eog_channels = NULL,
  eeg_channels = NULL,
  verbose = TRUE
)
}
\arguments{
\item{psg}{An \code{mrpheus_psg} object from \code{\link[=prepare_psg]{prepare_psg()}}.}

\item{eog_channels}{Character vector. EOG channel labels to use as
regressors. If \code{NULL} (default), all channels of type \code{"EOG"} in
\code{psg$channel_map} are used.}

\item{eeg_channels}{Character vector. EEG channel labels to clean. If
\code{NULL} (default), all non-bad \code{"EEG"} channels are used.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
A new \code{mrpheus_psg} with cleaned EEG signals and re-segmented
epochs. EOG and other channels are unchanged.
}
\description{
For each EEG channel, fits a multiple linear regression on all EOG reference
channels and subtracts the fitted EOG component, leaving the residual as the
cleaned signal. This removes both transient blink artefacts and slow ocular
drift without requiring blink event detection.
}
\details{
This is the pure-R equivalent of MNE's \code{compute_proj_eog()} / SSP
projection pipeline. Both approaches project out the subspace spanned by
the EOG channels; regression does so directly on the continuous signal.
}
\examples{
\dontrun{
rec   <- read_edf("psg.edf")
psg   <- prepare_psg(rec) |> preprocess_psg()
clean <- correct_eog_regression(psg)
}
}
