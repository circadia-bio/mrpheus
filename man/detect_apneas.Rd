% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_apneas.R
\name{detect_apneas}
\alias{detect_apneas}
\title{Detect respiratory events (apneas and hypopneas)}
\usage{
detect_apneas(
  psg,
  airflow_channel = NULL,
  spo2_channel = NULL,
  min_duration_s = 10,
  apnea_threshold = 0.9,
  hypopnea_threshold = 0.3,
  desaturation_threshold = 3
)
}
\arguments{
\item{psg}{An \code{mrpheus_psg} object from \code{\link[=prepare_psg]{prepare_psg()}}.}

\item{airflow_channel}{Character. Airflow channel label (e.g. nasal pressure
or thermistor). If \code{NULL} (default), the first RESP channel is used.}

\item{spo2_channel}{Character or \code{NULL}. SpO2 channel for hypopnea scoring.}

\item{min_duration_s}{Numeric. Minimum event duration in seconds. Default \code{10}.}

\item{apnea_threshold}{Numeric. Fractional reduction required for apnea
classification. Default \code{0.90}.}

\item{hypopnea_threshold}{Numeric. Fractional reduction required for
hypopnea classification. Default \code{0.30}.}

\item{desaturation_threshold}{Numeric. SpO2 drop (percentage points) required
to confirm hypopnea. Default \code{3}.}
}
\value{
A list with:
\describe{
\item{events}{Tibble. One row per event: \code{epoch}, \code{start_s}, \code{end_s},
\code{duration_s}, \code{type} (\code{"apnea"} / \code{"hypopnea"}), \code{desaturation}.}
\item{summary}{Tibble. \code{n_apneas}, \code{n_hypopneas}, \code{n_events}.}
}
}
\description{
Detects apneas and hypopneas in respiratory airflow and effort signals,
returning event-level metadata and summary indices. Detection follows AASM
2012/2017 criteria: >= 90\% signal reduction for >= 10 s (apnea), or >= 30\%
reduction with >= 3\% SpO2 desaturation or arousal for >= 10 s (hypopnea).
}
\seealso{
\code{\link[=compute_ahi]{compute_ahi()}}, \code{\link[=compute_odi]{compute_odi()}}
}
