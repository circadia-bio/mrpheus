% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_spectrogram.R
\name{compute_spectrogram}
\alias{compute_spectrogram}
\title{Compute a time-frequency spectrogram}
\usage{
compute_spectrogram(
  psg,
  channel = NULL,
  freq_range = c(0, 40),
  window_s = 2,
  overlap = 0.5,
  db = TRUE
)
}
\arguments{
\item{psg}{An \code{mrpheus_psg} object from \code{\link[=prepare_psg]{prepare_psg()}}.}

\item{channel}{Character. A single channel label.}

\item{freq_range}{Numeric vector of length 2. Frequency range to return
(Hz). Default \code{c(0, 40)}.}

\item{window_s}{Numeric. STFT window length in seconds. Default \code{2}.}

\item{overlap}{Numeric in [0, 1). Fractional window overlap. Default \code{0.5}.}

\item{db}{Logical. Return power in decibels (\code{10 * log10(power)})?
Default \code{TRUE}.}
}
\value{
A list of class \code{mrpheus_spectrogram} with:
\describe{
\item{power}{Numeric matrix. Rows = epochs, columns = frequency bins.}
\item{freqs}{Numeric vector. Frequency bin centres (Hz).}
\item{epochs}{Integer vector. Epoch indices.}
\item{channel}{Character. The channel label.}
\item{db}{Logical. Whether power is in dB.}
}
}
\description{
Generates a short-time Fourier transform (STFT) based spectrogram for a
single PSG channel across all epochs. Returns power (µV²/Hz) as a matrix
with time (epochs) on rows and frequency on columns.
}
