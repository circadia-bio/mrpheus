% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signal_filters.R
\name{bandpass_filter}
\alias{bandpass_filter}
\title{Apply a bandpass filter to a signal}
\usage{
bandpass_filter(signal, sr, low_hz, high_hz)
}
\arguments{
\item{signal}{Numeric vector.}

\item{sr}{Numeric. Sampling rate in Hz.}

\item{low_hz}{Numeric. Low cutoff frequency in Hz. Use \code{0} or negative to
apply a low-pass filter only.}

\item{high_hz}{Numeric. High cutoff frequency in Hz.}
}
\value{
Filtered numeric vector, same length as \code{signal}.
}
\description{
Designs a 4th-order Butterworth bandpass filter and applies it zero-phase
using \code{gsignal::filtfilt()}. Handles edge cases gracefully:
}
\details{
\itemize{
\item \code{low_hz <= 0} → low-pass at \code{high_hz}
\item \code{high_hz >= Nyquist} → high-pass at \code{low_hz}
\item Otherwise → bandpass between \code{low_hz} and \code{high_hz}
}

The \code{high_hz} cutoff is clamped to 99 \% of Nyquist to avoid instability.
Because this uses a short IIR filter, it is safe on long recordings.
}
\examples{
\dontrun{
# Standard EEG bandpass
filtered <- bandpass_filter(eeg_signal, sr = 256, low_hz = 0.3, high_hz = 35)

# High-pass only (baseline drift removal)
hp <- bandpass_filter(eeg_signal, sr = 256, low_hz = 0.1, high_hz = Inf)
}
}
