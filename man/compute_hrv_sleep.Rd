% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_hrv_sleep.R
\name{compute_hrv_sleep}
\alias{compute_hrv_sleep}
\title{Compute HRV metrics across sleep stages}
\usage{
compute_hrv_sleep(
  psg,
  staging = NULL,
  ecg_channel = NULL,
  min_rr_ms = 300,
  max_rr_ms = 2000
)
}
\arguments{
\item{psg}{An \code{mrpheus_psg} object from \code{\link[=prepare_psg]{prepare_psg()}}.}

\item{staging}{Tibble from \code{\link[=stage_epochs]{stage_epochs()}} or \code{NULL}. If \code{NULL},
HRV is computed across all epochs without stage stratification.}

\item{ecg_channel}{Character or \code{NULL}. ECG channel label. If \code{NULL}
(default), the first non-bad ECG channel is used.}

\item{min_rr_ms}{Numeric. Minimum physiologically plausible RR interval (ms).
Default \code{300} (200 bpm ceiling).}

\item{max_rr_ms}{Numeric. Maximum physiologically plausible RR interval (ms).
Default \code{2000} (30 bpm floor).}
}
\value{
A tibble with one row per sleep stage (or one row if \code{staging} is
\code{NULL}):
\describe{
\item{stage}{Character. AASM stage or \code{"ALL"}.}
\item{n_epochs}{Integer. Number of epochs in this stage.}
\item{mean_rr_ms}{Numeric. Mean RR interval (ms).}
\item{sdnn_ms}{Numeric. SDNN — SD of all NN intervals.}
\item{rmssd_ms}{Numeric. RMSSD — root mean square of successive differences.}
\item{lf_power}{Numeric. LF band power (0.04–0.15 Hz).}
\item{hf_power}{Numeric. HF band power (0.15–0.4 Hz).}
\item{lf_hf_ratio}{Numeric. LF/HF ratio.}
}
}
\description{
Extracts R-peak positions from the ECG channel, computes standard time- and
frequency-domain heart rate variability (HRV) metrics, and stratifies results
by sleep stage.
}
