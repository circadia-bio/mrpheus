% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correct_eog.R
\name{correct_eog_ica}
\alias{correct_eog_ica}
\title{Remove EOG artefacts using ICA}
\usage{
correct_eog_ica(
  psg,
  eog_channels = NULL,
  eeg_channels = NULL,
  n_components = NULL,
  threshold = 0.35,
  fun = "logcosh",
  verbose = TRUE
)
}
\arguments{
\item{psg}{An \code{mrpheus_psg} object from \code{\link[=prepare_psg]{prepare_psg()}}.}

\item{eog_channels}{Character vector. EOG channel labels used as the
artifact reference. If \code{NULL} (default), all \code{"EOG"} channels are used.}

\item{eeg_channels}{Character vector. EEG channels to decompose and clean.
If \code{NULL} (default), all non-bad \code{"EEG"} channels are used.}

\item{n_components}{Integer or \code{NULL}. Number of ICA components. \code{NULL}
(default) uses \code{min(6L, n_eeg_channels)}, matching the notebook.}

\item{threshold}{Numeric. Absolute Pearson correlation threshold above
which a component is flagged as EOG-related. Default \code{0.35}.}

\item{fun}{Character. Contrast function passed to \code{\link[fastICA:fastICA]{fastICA::fastICA()}}.
\code{"logcosh"} (default) or \code{"exp"}. The R method is used internally for
robustness across channel counts; this is slower than the C backend but
avoids matrix-conformality errors on small channel sets.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
A new \code{mrpheus_psg} with cleaned EEG signals and re-segmented
epochs. EOG and other channels are unchanged.
}
\description{
Decomposes the EEG into independent components via FastICA, identifies
components whose time courses correlate with the EOG channels above
\code{threshold}, subtracts their contribution from the EEG, and returns a
cleaned \code{mrpheus_psg}. Mirrors MNE's ICA EOG rejection pipeline with
default settings matching the companion Python notebook
(\code{n_components = min(6, n_eeg)}, \code{threshold = 0.35}).
}
\details{
\strong{Note:} FastICA (this implementation) and MNE's default InfoMax ICA find
different decompositions; the identified components and final signal will
therefore differ from MNE output, but artifact removal performance is
comparable.
}
\examples{
\dontrun{
rec   <- read_edf("psg.edf")
psg   <- prepare_psg(rec) |> preprocess_psg()
clean <- correct_eog_ica(psg)

# Stricter threshold
clean <- correct_eog_ica(psg, threshold = 0.5)
}
}
