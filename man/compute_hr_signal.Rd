% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_hr_signal.R
\name{compute_hr_signal}
\alias{compute_hr_signal}
\title{Compute Instantaneous Heart Rate from R-Peak Indices}
\usage{
compute_hr_signal(qrs)
}
\arguments{
\item{qrs}{A \code{mrpheus_qrs} object from \code{\link[=detect_qrs]{detect_qrs()}}, or a
named list with at least \code{qrs_i} (integer vector of R-peak indices) and
\code{fs} (sampling frequency in Hz).}
}
\value{
Numeric vector of length \code{max(qrs$qrs_i)}. Each sample contains
the instantaneous heart rate in beats per minute for the inter-beat
interval it falls within. Samples before the first detected R-peak are 0.
}
\description{
Converts R-peak sample indices (from \code{\link[=detect_qrs]{detect_qrs()}}) into a
sample-by-sample instantaneous heart rate trace by filling each inter-beat
interval with the constant HR derived from that interval's length.
}
\examples{
\dontrun{
rec <- read_philips_physlog("sub-01_physlog.log")
qrs <- detect_qrs(rec$C[, "v1raw"], fs = rec$HDR$sfreq)
hr  <- compute_hr_signal(qrs)

plot(seq_along(hr) / rec$HDR$sfreq, hr, type = "l",
     xlab = "Time (s)", ylab = "HR (bpm)")
}
}
\seealso{
\code{\link[=detect_qrs]{detect_qrs()}}, \code{\link[=compute_hrv_sleep]{compute_hrv_sleep()}}
}
