% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess_psg.R
\name{preprocess_psg}
\alias{preprocess_psg}
\title{Preprocess a PSG recording}
\usage{
preprocess_psg(
  psg,
  channel_rename = NULL,
  dc = TRUE,
  powerline_freq = NULL,
  notch_harmonics = TRUE,
  notch_bw_hz = 2,
  eeg_bandpass = c(0.3, 35),
  eog_bandpass = c(0.3, 15),
  emg_bandpass = c(10, 99),
  ecg_bandpass = c(0.5, 40),
  verbose = TRUE
)
}
\arguments{
\item{psg}{An \code{mrpheus_psg} object from \code{\link[=prepare_psg]{prepare_psg()}}.}

\item{channel_rename}{Named character vector mapping old channel labels to
new labels, e.g. \code{c("C3-A2" = "C3", "O2-A1" = "O2")}. Applied before
filtering; updates \code{channel_map} labels accordingly. Default \code{NULL}.}

\item{dc}{Logical. Remove per-channel DC offset via \code{\link[=remove_dc]{remove_dc()}}.
Default \code{TRUE}.}

\item{powerline_freq}{Integer or \code{NULL}. Powerline frequency in Hz (\code{50L}
or \code{60L}). \code{NULL} (default) triggers auto-detection via
\code{\link[=detect_powerline]{detect_powerline()}} on the first clean EEG channel.}

\item{notch_harmonics}{Logical. Notch harmonics of \code{powerline_freq} up to
Nyquist - 5 Hz. Default \code{TRUE}.}

\item{notch_bw_hz}{Numeric. Full bandwidth of each notch in Hz. Default \code{2}.}

\item{eeg_bandpass}{Numeric vector of length 2. Bandpass limits (Hz) applied
to all EEG channels. Default \code{c(0.3, 35)}.}

\item{eog_bandpass}{Numeric vector of length 2. Bandpass limits (Hz) applied
to all EOG channels. Default \code{c(0.3, 15)}.}

\item{emg_bandpass}{Numeric vector of length 2. Bandpass limits (Hz) applied
to all EMG channels. Default \code{c(10, 99)}.}

\item{ecg_bandpass}{Numeric vector of length 2. Bandpass limits (Hz) applied
to all ECG channels. Default \code{c(0.5, 40)}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
A new \code{mrpheus_psg} object with filtered signals and re-segmented
epochs. The \code{edf$signals} entries are replaced in-place with the filtered
continuous signals so that subsequent epoch access is consistent.
}
\description{
Applies the standard sleep EEG preprocessing pipeline to an \code{mrpheus_psg}
object: optional channel renaming, DC offset removal, powerline notch
filtering (plus harmonics), and channel-type-specific bandpass filtering.
}
\details{
Filtering is applied to the full \strong{continuous} signal stored in
\code{psg$edf$signals} before re-epoching, which avoids filter discontinuities
at epoch boundaries. The returned object is a new \code{mrpheus_psg} with the
same epoch length and channel map as the input.

The individual filter steps (\code{\link[=remove_dc]{remove_dc()}}, \code{\link[=detect_powerline]{detect_powerline()}},
\code{\link[=notch_filter]{notch_filter()}}, \code{\link[=bandpass_filter]{bandpass_filter()}}) are exported separately and can be
called on any numeric vector without constructing a PSG object.
}
\examples{
\dontrun{
rec  <- read_edf("data/psg_001.edf")
psg  <- prepare_psg(rec)

# Auto-detect powerline, apply standard filters
psg_clean <- preprocess_psg(psg)

# Rename linked-ear channels to 10-20 names, fix powerline explicitly
psg_clean <- preprocess_psg(
  psg,
  channel_rename = c("C3-A2" = "C3", "C4-A1" = "C4",
                     "O1-A2" = "O1", "O2-A1" = "O2"),
  powerline_freq = 50L
)
}
}
