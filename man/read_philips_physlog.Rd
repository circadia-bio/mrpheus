% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_philips_physlog.R
\name{read_philips_physlog}
\alias{read_philips_physlog}
\title{Read a Philips MRI Physiological Log File}
\usage{
read_philips_physlog(
  path,
  system = c("wBTU", "wired", "custom"),
  sfreq = NULL,
  channels = NULL,
  skipprep = FALSE
)
}
\arguments{
\item{path}{Character. Path to the Philips \code{.log} file.}

\item{system}{Philips PMU hardware preset. One of:
\describe{
\item{\code{"wBTU"} (default)}{Wireless VCG system, 496 Hz. 10-channel
layout: v1raw, v2raw, v1, v2, ppu, resp, gx, gy, gz, mark.
Used with the Philips Achieva/Ingenia dStream wireless body
telemetry unit.}
\item{\code{"wired"}}{Older wired ECG system, 500 Hz. Fewer channels
(no accelerometer); layout read from file header.}
\item{\code{"custom"}}{Supply \code{sfreq} explicitly.}
}}

\item{sfreq}{Numeric or \code{NULL}. Sampling frequency in Hz. Overrides the
\code{system} preset when supplied. Required when \code{system = "custom"}.}

\item{channels}{Character vector or \code{NULL}. Channel names to return.
\code{NULL} (default) returns all channels. \code{"none"} returns no signal data
(markers only).}

\item{skipprep}{Logical. If \code{TRUE}, skip samples recorded before the blank
\verb{#} line that marks the end of the preparation phase. Default \code{FALSE}.}
}
\value{
A list of class \code{mrpheus_physlog} with components:
\describe{
\item{\code{C}}{Integer matrix (samples × channels). Column order matches the
file header.}
\item{\code{M}}{Two-column integer matrix: \code{marker} (bit-encoded value) and
\code{index} (1-based sample number) for every non-zero marker.}
\item{\code{I}}{Named list of 1-based sample index vectors for each event
type: VcgOnset, PpuOnset, TriggerResp, Measurement, ScannerStart,
ScannerStop, TriggerExt, Calibration, RefTriggerVcg.}
\item{\code{HDR}}{List of header metadata: ID, DATETIME, STATS,
DockableTable, COLUMN_NAMES, system, sfreq.}
\item{\code{path}}{Character. Resolved path to the source file.}
}
}
\description{
Reads a Philips PMU physiological log (\code{.log}) file recorded alongside an
MRI acquisition and returns a structured object containing the signal
matrix, event markers, and recording metadata. Supports all Philips PMU
hardware variants via the \code{system} preset.
}
\details{
Bit-encoded marker values:

\if{html}{\out{<div class="sourceCode">}}\preformatted{0x0001  ECG trigger       0x0002  PPU trigger      0x0004  respiration
0x0008  slice onset       0x0010  scanner start    0x0020  scanner stop
0x0040  external trigger  0x0080  calibration      0x8000  ref ECG trigger
}\if{html}{\out{</div>}}

Use the scanner-stop marker (more reliable than start) to time-lock data:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{stopidx  <- tail(data$I$ScannerStop, 1) - enddelay
startidx <- stopidx - round(n_vols * TR * data$HDR$sfreq)
epoch    <- data$C[startidx:stopidx, ]
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
# Philips Achieva/Ingenia with wireless VCG (default)
rec <- read_philips_physlog("sub-01_ses-01_physlog.log")
rec

# Older wired scanner
rec <- read_philips_physlog("sub-01_physlog.log", system = "wired")

# Custom sampling rate
rec <- read_philips_physlog("sub-01_physlog.log", system = "custom", sfreq = 400)

# ECG channels only
rec <- read_philips_physlog("sub-01_physlog.log", channels = c("v1raw", "v2raw"))
}
}
\seealso{
\code{\link[=detect_qrs]{detect_qrs()}}, \code{\link[=compute_hr_signal]{compute_hr_signal()}}
}
