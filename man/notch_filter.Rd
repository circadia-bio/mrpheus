% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signal_filters.R
\name{notch_filter}
\alias{notch_filter}
\title{Apply a notch (bandstop) filter to a signal}
\usage{
notch_filter(signal, sr, freq, bandwidth_hz = 2, harmonics = TRUE)
}
\arguments{
\item{signal}{Numeric vector.}

\item{sr}{Numeric. Sampling rate in Hz.}

\item{freq}{Numeric. Centre frequency in Hz (e.g. \code{50} or \code{60}).}

\item{bandwidth_hz}{Numeric. Full bandwidth of the notch in Hz. Default \code{2}.}

\item{harmonics}{Logical. Also notch harmonics (2×, 3×, ...) below
Nyquist − 5 Hz. Default \code{TRUE}.}
}
\value{
Filtered numeric vector, same length as \code{signal}.
}
\description{
Designs a 2nd-order Butterworth bandstop filter centred at \code{freq} Hz with
bandwidth \code{bandwidth_hz} and applies it zero-phase using
\code{gsignal::filtfilt()}. Because the filter has only ~9 coefficients, it is
safe to use on long recordings without memory issues.
}
\details{
By default also applies the filter at all harmonics of \code{freq} below the
Nyquist limit, which is the standard approach for powerline noise removal.
}
\examples{
\dontrun{
clean <- notch_filter(eeg_signal, sr = 256, freq = 50)
clean <- notch_filter(eeg_signal, sr = 256, freq = 60, harmonics = FALSE)
}
}
