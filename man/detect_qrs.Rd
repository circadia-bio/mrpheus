% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_qrs.R
\name{detect_qrs}
\alias{detect_qrs}
\title{Detect QRS Complexes in an ECG Signal (Pan-Tompkins Algorithm)}
\usage{
detect_qrs(ecg, fs)
}
\arguments{
\item{ecg}{Numeric vector. Raw ECG signal.}

\item{fs}{Numeric. Sampling frequency in Hz. If \code{ecg} comes from
\code{\link[=read_philips_physlog]{read_philips_physlog()}}, use \code{rec$HDR$sfreq}.}
}
\value{
A list of class \code{mrpheus_qrs} with components:
\describe{
\item{\code{qrs_i}}{Integer vector of R-peak sample indices (1-based),
referenced to the bandpass-filtered signal.}
\item{\code{qrs_amp}}{Numeric vector of R-wave amplitudes at each detected
peak (normalised bandpass signal units).}
\item{\code{delay}}{Numeric. Sample delay introduced by the moving-average
stage (half the 150 ms window length).}
\item{\code{fs}}{Numeric. Sampling frequency passed to the function.}
}
}
\description{
Detects R-peaks in a raw ECG signal using the Pan-Tompkins algorithm.
Implements bandpass filtering, derivative, squaring, moving-average
integration, and adaptive dual-threshold peak detection with T-wave
rejection and search-back for missed beats.
}
\details{
Processing pipeline (Pan & Tompkins, 1985):
\enumerate{
\item \strong{Bandpass filter} 5–15 Hz (Butterworth N=3). At fs = 200 Hz,
implemented as separate 12 Hz low-pass then 5 Hz high-pass stages.
\item \strong{Derivative filter} — 5-point kernel interpolated to match fs.
\item \strong{Squaring} — non-linear emphasis of dominant peaks.
\item \strong{150 ms moving-average integration.}
\item \strong{Adaptive thresholding} — dual-threshold scheme with T-wave rejection
and search-back for missed beats (initialised on 2-second training window).
}

R-peak indices are returned relative to the bandpass-filtered signal.
Account for \code{delay} if you need indices aligned to the raw ECG.
}
\examples{
\dontrun{
rec  <- read_philips_physlog("sub-01_physlog.log")
qrs  <- detect_qrs(rec$C[, "v1raw"], fs = rec$HDR$sfreq)
qrs

# Downstream HR signal
hr <- compute_hr_signal(qrs)
}
}
\references{
Pan J, Tompkins WJ. A real-time QRS detection algorithm.
\emph{IEEE Trans Biomed Eng.} 1985;32(3):230–236.
\doi{10.1109/TBME.1985.325532}
}
\seealso{
\code{\link[=read_philips_physlog]{read_philips_physlog()}}, \code{\link[=compute_hr_signal]{compute_hr_signal()}},
\code{\link[=compute_hrv_sleep]{compute_hrv_sleep()}}
}
